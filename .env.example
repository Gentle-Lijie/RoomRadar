# 复制为 .env 后按需修改；.env 不会提交到仓库。

# ---- 端口 ----
# 前端（静态站点）监听端口
FRONTEND_PORT=8080
# 后端 API 监听端口
BACKEND_PORT=3001

# ---- 可选 ----
# 后端公开地址（跨机部署时填写；默认用 http://127.0.0.1:<BACKEND_PORT>）
# BACKEND_HOST=http://127.0.0.1:3001

# pm2 进程名前缀
PM2_APP_NAME=unnc-room-check
